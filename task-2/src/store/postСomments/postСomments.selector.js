export const selectPostComments = (state) => state.postComments.postComments;

export const selectPostCommentsIsLoading = (state) => state.postComments.isLoading;

export const selectPostCommentsError = (state) => state.postComments.error;
